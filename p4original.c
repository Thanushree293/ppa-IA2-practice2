#include<stdio.h>
int input_array_size()
{
  int x;
  printf("enter the the size of an array\n");
  scanf("%d",&x);
  return x;
  }
  void input_array(int n,int a[n],int c)
  {
    printf("enter the array values\n");
    int count=0;
    for(i=1;i<=a[n];i++)
    {
      if(n%i=0)
    }
  }